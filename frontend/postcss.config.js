// PostCSS configuration file
export default {
  plugins: {
    // Processes Tailwind's @tailwind directives and utility classes into actual CSS
    tailwindcss: {},
    // Automatically adds vendor prefixes (e.g. -webkit-, -moz-) for browser compatibility
    autoprefixer: {},
  },
}
